import { useState } from 'react'
import { Button } from './components'
function App() {
  return (
    <div>
      <Button title="niebieski" onClick={() => null} />
    </div>
  )
}

export default App
