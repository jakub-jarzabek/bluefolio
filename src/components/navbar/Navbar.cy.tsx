/// <reference types="Cypress" />
import React from 'react';
import { Navbar } from './Navbar'


describe('Password input', () => {
    beforeEach(() => {
        cy.mount(<Navbar />)
      })
      it('mounts', () => {
      })
    })